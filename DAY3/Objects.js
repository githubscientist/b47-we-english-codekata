let person = {
    name: 'krish',
    age: 25,
    salary: 30000,
};

// console.log(person);

// console.log(person['salary']);

// person.phone = '9080706050';
// person['isEligible'] = true;

// delete person.salary;

// console.log(person);

// traverse the object
// console.log(Object.entries(person));

// for (let value of Object.keys(person)) {
//     console.log(person[value]);
// }

